/*=============================================================================
	DeusEx_ClassBoilerplate_Reconstructed_Reconstructed.cpp
	Small native class boilerplate restored before the compile phase.

	No runtime feature work lives here.  This file only closes explicit native
	constructors declared by the original DeusEx headers, so the later VC98 pass
	does not trip over missing class symbols before we even reach gameplay code.
=============================================================================*/

#include "DeusExReconstructedPrivate.h"

ADeusExLevelInfo::ADeusExLevelInfo()
{
	guard(ADeusExLevelInfo::ADeusExLevelInfo);
	unguard;
}

/*-----------------------------------------------------------------------------
	The End.
-----------------------------------------------------------------------------*/
